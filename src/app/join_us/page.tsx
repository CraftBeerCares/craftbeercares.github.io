import { PageTemplate } from "@/components";
import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Join Us",
};

export default function Page() {
  return <PageTemplate>Join Us</PageTemplate>;
}
